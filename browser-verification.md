# ผลตรวจสอบ live preview

ตรวจสอบหน้าเว็บบน dev preview วันที่ 14 สิงหาคม 2026 หลังเพิ่มระบบภาษาและ Modal กิจกรรม

| จุดตรวจ | ผลลัพธ์ |
| --- | --- |
| หน้าเริ่มต้น | เปิดเป็นภาษาไทยตาม requirement และแสดงภาพประกอบเดิมครบ |
| สวิตช์ภาษา | กด EN แล้วข้อความ navbar, hero, ประวัติ, กิจกรรม, ข้อปฏิบัติ, แกลเลอรี และ footer เปลี่ยนเป็นภาษาอังกฤษ |
| สวิตช์กลับภาษา | มีปุ่ม TH/EN พร้อมสถานะ active และรองรับการกดกลับเป็นภาษาไทย |
| Modal กิจกรรม | เปิดจากปุ่มกิจกรรมได้ แสดงภาพเดิม คำอธิบายสั้น หัวข้อ “How to join” และขั้นตอน 3 ข้อ |
| Responsive | ตรวจ screenshot desktop 1280px และ mobile 390px แล้ว ไม่พบแถบเมนูล้นหรือภาพหลักเสียสัดส่วน |
| Build | `pnpm check` และ `pnpm build` ผ่าน ไม่มี TypeScript error |

## Standalone

| จุดตรวจ | ผลลัพธ์ |
| --- | --- |
| เปิดไฟล์โดยตรง | เปิด `songkran-online-standalone-fixed.html` ผ่าน `file://` ได้ และ assets สาธารณะโหลดจาก URL เดิม |
| ภาษาอังกฤษ | กด EN แล้ว title, meta description, navbar, hero, ประวัติย่อ, กิจกรรม, ข้อปฏิบัติ, แกลเลอรี และ footer เปลี่ยนเป็นภาษาอังกฤษ |
| ภาษาเริ่มต้น | เปิดไฟล์ใหม่เป็นภาษาไทยและปุ่ม TH active |
| คงดีไซน์ | สี ภาพประกอบ โครงหน้า และ responsive modal ยังคงใช้ CSS เดิม เพิ่มเฉพาะสวิตช์ภาษาและกล่องขั้นตอน |
| Modal ภาษาอังกฤษ | จำลองคลิกปุ่มกิจกรรมแล้วพบ Modal จริง พร้อมชื่อ “Offer Water and Ask for Blessings” และขั้นตอน “How to join” ครบ 3 ขั้นตอน |
| React desktop | หน้า hero, navbar สองชั้น, TH/EN toggle และภาพหลักแสดงครบ โทนสีน้ำเงิน-ทองและองค์ประกอบโปสเตอร์เดิมยังอยู่ |
| React mobile | สวิตช์ TH/EN และเมนู hamburger อยู่ในแถบเมนูโดยไม่ล้น ภาพ hero และปุ่มหลักจัดเรียงแนวตั้งได้สมบูรณ์ |

## การปรับระเบียบข้อปฏิบัติเป็น 10 ข้อ

- React production build ผ่านด้วย `pnpm check && pnpm build`
- หน้า React แสดงการ์ดระเบียบครบ 10 ข้อบน desktop และ mobile โดยไม่เปลี่ยนธีม สี ภาพ หรือโครงสร้างหลัก
- standalone มีการ์ด `.rule-card` ครบ 10 รายการ และมีข้อมูลภาษาไทย/อังกฤษครบตามลำดับเดียวกัน
- หัวข้อและคำเกริ่นของส่วนระเบียบอัปเดตตามเนื้อหาใหม่ทั้งสองภาษา

## การปรับประวัติความเป็นมาเป็นเนื้อหาใหม่

- React ใช้เนื้อหาใหม่ 4 ส่วน ได้แก่ วันสงกรานต์, ความเป็นมา, ความสำคัญของวันสงกรานต์ และสงกรานต์กับ UNESCO ทั้งในสรุปหน้าแรกและ Modal อ่านเพิ่มเติม
- standalone ใช้ชุดข้อมูลประวัติ 4 ส่วนเดียวกับ React และมีคำแปลภาษาอังกฤษสอดคล้องกัน
- `pnpm check` และ `pnpm build` ผ่านหลังแก้ไข โดยมีเพียงคำเตือนเดิมของ Vite เรื่อง assets `/manus-storage/` และขนาด bundle
- ตรวจ screenshot desktop 1280px และ mobile 375px แล้ว ภาพ สี กรอบโปสเตอร์ และ responsive layout เดิมยังคงอยู่
