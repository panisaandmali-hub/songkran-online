# คู่มือแก้ไขข้อมูลเว็บไซต์สงกรานต์ออนไลน์

คู่มือนี้อธิบายวิธีแก้ **ข้อความ รูปภาพ และรายละเอียดกิจกรรม** ในไฟล์ `client/src/pages/Home.tsx` โดยเน้นการแก้เฉพาะข้อมูล ไม่เปลี่ยนโครงสร้าง สี การจัดวาง หรือ animation ของเว็บไซต์

## 1. หลักการสำคัญก่อนแก้ไฟล์

ก่อนแก้ไข แนะนำให้คัดลอกไฟล์ `Home.tsx` เก็บไว้เป็นไฟล์สำรอง เช่น `Home.backup.tsx` หรือดาวน์โหลดเวอร์ชันปัจจุบันจากหน้า Code ของโปรเจกต์

ถ้าต้องการเปลี่ยนข้อมูลอย่างปลอดภัย ให้แก้เฉพาะข้อความที่อยู่ในเครื่องหมายคำพูด เช่น `"ข้อความที่ต้องการแก้"` และแก้เฉพาะ URL รูปภาพในส่วน `ASSETS` อย่าลบชื่อ `id`, ชื่อ class เช่น `water-card`, `elder-card` หรือโครงสร้าง JSX หากยังไม่คุ้นเคยกับ React เพราะชื่อเหล่านี้ควบคุมการจัดวางและสไตล์ของหน้าเว็บ

## 2. จุดรวมไฟล์รูปภาพ: ส่วน `ASSETS`

ช่วงต้นของไฟล์มีวัตถุชื่อ `ASSETS` ประมาณนี้:

```tsx
const ASSETS = {
  logo: "/manus-storage/songkran-mark_df73cd15.png",
  hero: "/manus-storage/songkran-hero-art_bdd15d42.png",
  water: "/manus-storage/songkran-water-activity_096e2bf2.png",
  elders: "/manus-storage/songkran-elder-blessing_836acaa4.png",
  temple: "/manus-storage/songkran-temple-sand_ef5cfe26.png",
};
```

หากต้องการเปลี่ยนรูป ให้เปลี่ยนเฉพาะค่าหลังเครื่องหมาย `:` เช่น:

```tsx
const ASSETS = {
  logo: "/รูปภาพโลโก้ใหม่.png",
  hero: "/รูปภาพแบนเนอร์ใหม่.png",
  water: "/รูปกิจกรรมสาดน้ำใหม่.png",
  elders: "/รูปกิจกรรมรดน้ำดำหัวใหม่.png",
  temple: "/รูปวัดหรือก่อเจดีย์ทรายใหม่.png",
};
```

ในโปรเจกต์นี้ รูปภาพสำหรับเว็บไซต์ควรอัปโหลดผ่านระบบจัดการไฟล์ของโปรเจกต์ก่อน แล้วนำ URL ที่ระบบให้มาใส่ใน `ASSETS` ห้ามใส่ path จากคอมพิวเตอร์ส่วนตัว เช่น `C:\Users\ชื่อ\รูป.png` เพราะเว็บไซต์ที่เผยแพร่แล้วจะเปิดไฟล์นั้นไม่ได้

หากใช้รูปเดิมซ้ำหลายตำแหน่ง ให้ใช้ชื่อใน `ASSETS` เดิม เช่น `ASSETS.temple` การเปลี่ยนรูปที่ `ASSETS.temple` จะเปลี่ยนทุกจุดที่อ้างถึงรูปนั้น

## 3. แก้ชื่อกิจกรรมและรายละเอียด

ข้อมูลกิจกรรมหลักอยู่ในตัวแปร `activities` หลังส่วน `ASSETS` ตัวอย่างหนึ่งรายการ:

```tsx
{
  id: "water",
  title: "สาดน้ำ & ปะแป้ง",
  kicker: "สนุก สุขใจ",
  description: "เล่นน้ำอย่างสุภาพ เติมรอยยิ้มให้คนในชุมชน และเริ่มต้นปีใหม่ด้วยความสดชื่น",
  image: ASSETS.water,
  accent: "water",
  icon: <Waves size={20} />,
  details: [
    "น้ำมีความหมายถึงการชำระล้างสิ่งไม่ดี การเริ่มต้นใหม่ และความเป็นสิริมงคล",
    "การปะแป้งเป็นกิจกรรมร่วมสมัย ควรขออนุญาตและเล่นอย่างสุภาพกับทุกคน",
    "เลือกพื้นที่เล่นน้ำที่จัดไว้ และดูแลเด็ก ผู้สูงอายุ รวมถึงผู้ใช้รถใช้ถนน",
  ],
},
```

แก้ได้ตามหน้าที่ดังนี้:

| ต้องการเปลี่ยน | แก้ที่ตัวแปร | ผลที่เกิดขึ้น |
|---|---|---|
| ชื่อกิจกรรม | `title` | เปลี่ยนหัวข้อบนการ์ดและ modal |
| ข้อความสั้นเหนือชื่อ | `kicker` | เปลี่ยนคำโปรยของกิจกรรม |
| คำอธิบาย | `description` | เปลี่ยนคำอธิบายใน modal และส่วนข้อมูล |
| รูปภาพของกิจกรรม | `image` | เปลี่ยนภาพใน modal และส่วนที่เรียกใช้กิจกรรมนั้น |
| รายละเอียดแบบรายการ | `details` | เปลี่ยน bullet ในหน้าต่างรายละเอียด |

ตัวอย่างการเปลี่ยนข้อมูลกิจกรรมรดน้ำดำหัว:

```tsx
{
  id: "elder",
  title: "รดน้ำขอพรผู้ใหญ่ในชุมชน",
  kicker: "อบอุ่นและเคารพ",
  description: "ร่วมแสดงความกตัญญู ขอพร และพบปะผู้ใหญ่ที่เรารักในวันปีใหม่ไทย",
  image: ASSETS.elders,
  accent: "coral",
  icon: <HeartHandshake size={20} />,
  details: [
    "จัดเตรียมน้ำอบ น้ำสะอาด และพวงมาลัยตามความเหมาะสม",
    "กล่าวคำอวยพรและขอขมาอย่างสุภาพ",
    "เปิดโอกาสให้ทุกวัยได้พูดคุยและใช้เวลาร่วมกัน",
  ],
},
```

## 4. แก้ข้อความที่แสดงบนการ์ดกิจกรรมโดยตรง

ในส่วน `return` ของ `Home()` จะมี JSX สำหรับการ์ดกิจกรรม เช่น:

```tsx
<h3>สาดน้ำ <span>&amp;</span> ปะแป้ง</h3>
<p>สนุก สุขใจ</p>
```

สามารถเปลี่ยนเป็น:

```tsx
<h3>เล่นน้ำอย่างปลอดภัย</h3>
<p>สนุกได้ ดูแลกันด้วย</p>
```

การ์ดกิจกรรมอื่น ๆ จะมีโครงสร้างใกล้เคียงกัน โดยมองหาข้อความที่อยู่ใน `<h3>` และ `<p>` แล้วแก้เฉพาะข้อความด้านใน

ปุ่มก็แก้ข้อความได้ เช่น:

```tsx
<button className="pill-button cyan" onClick={playWater}>
  <Waves size={18} /> สาดน้ำ
</button>
```

เปลี่ยนเฉพาะคำว่า `สาดน้ำ` ได้ แต่ควรคง `className`, `onClick={playWater}` และ `<Waves size={18} />` ไว้ เพื่อไม่ให้สี ปุ่ม และ animation หายไป

## 5. แก้ข้อความหน้า Hero และข้อความแนะนำงาน

ค้นหาส่วนที่มี `hero-copy`:

```tsx
<span className="hero-kicker">เทศกาลปีใหม่ไทย • ๒๕๖๙</span>
<h1>มาร่วมฉลอง <em>สงกรานต์</em><br />ตัวใส่ใหม่</h1>
<p>สนุก ปลอดภัย ส่งความสุขสไตล์ดิจิทัล<br />ร่วมกันรักษาประเพณีดี ๆ ให้คงอยู่ตลอดไป</p>
```

แก้ข้อความด้านในได้ตามต้องการ แต่ควรคง `<em>` และ `<br />` ไว้ เพราะ `<em>` ทำให้คำสำคัญเป็นสีทอง และ `<br />` ควบคุมการขึ้นบรรทัดให้เหมือนหน้าตาปัจจุบัน

ส่วนสรุปงานอยู่ใน `intro-copy`:

```tsx
<h2>สายน้ำเชื่อมใจ<br /><span>คนทุกวัยในชุมชน</span></h2>
<p>พื้นที่เล็ก ๆ บนโลกออนไลน์ที่รวมกิจกรรมสงกรานต์ ...</p>
```

สามารถเปลี่ยนข้อความใน `<h2>`, `<span>` และ `<p>` ได้ โดยคง tag เดิมไว้เพื่อรักษาสีและรูปแบบตัวอักษร

## 6. แก้ข้อปฏิบัติ

ข้อมูลข้อปฏิบัติอยู่ในตัวแปร `rules`:

```tsx
const rules = [
  {
    icon: <Droplets />,
    title: "เล่นน้ำอย่างสุภาพ",
    body: "ไม่นำน้ำสกปรก ไม่ฉีดน้ำแรง และไม่สาดใส่ผู้ที่ไม่ต้องการเล่น",
  },
];
```

แก้ `title` เป็นหัวข้อใหม่ และแก้ `body` เป็นคำอธิบายใหม่ได้ทันที ควรคง icon ที่เป็น component เช่น `<Droplets />`, `<Users />` หรือ `<HeartHandshake />` เอาไว้เพื่อให้ไอคอนยังแสดงผลตามเดิม

## 7. แก้ข้อความอวยพรในกล่องรดน้ำดำหัว

ค้นหาส่วน `wish-feed` จะพบข้อความตัวอย่าง เช่น:

```tsx
<b>น้องฟ้า</b><p>ขอให้สุขภาพแข็งแรงครับ!</p>
<b>วชัย</b><p>สุขสันต์วันสงกรานต์นะ!</p>
```

แก้ชื่อและข้อความได้โดยตรง เช่น:

```tsx
<b>คุณยายมาลี</b><p>ขอให้ทุกคนเดินทางปลอดภัยนะ</p>
```

ส่วนนี้เป็นข้อความประกอบหน้าเว็บ ไม่ใช่ระบบรับข้อความจากผู้ใช้งานจริง หากต้องการให้ผู้ใช้งานส่งคำอวยพรและบันทึกได้จริง จะต้องเพิ่มระบบฐานข้อมูลภายหลัง

## 8. แก้ชื่ออัลบั้มภาพ

ข้อมูลชื่อภาพอยู่ในตัวแปร `gallery`:

```tsx
const gallery = [
  { title: "สาดความสุข", className: "gallery-water" },
  { title: "ขอพรผู้ใหญ่", className: "gallery-elder" },
  { title: "ลานเจดีย์ทราย", className: "gallery-sand" },
];
```

แก้เฉพาะ `title` ได้ เช่น `ลานกิจกรรมชุมชนบ้านเรา` แต่ควรคง `className` เดิม เพราะ class เหล่านี้เชื่อมกับรูปภาพและสไตล์ใน `index.css`

## 9. สิ่งที่ไม่ควรแก้ถ้าต้องการคงหน้าตาเดิม

ไม่ควรแก้หรือลบ `className`, `id`, ชื่อตัวแปร `ASSETS`, `activities`, `rules`, `gallery`, ค่า `accent`, และฟังก์ชัน `playWater`, `jumpTo` หากต้องการเปลี่ยนเฉพาะข้อมูล

ไม่ควรเปลี่ยนโครงสร้าง tag เช่น เปลี่ยน `<button>` เป็น `<div>` หรือย้าย `<img>` ออกจากตำแหน่งเดิม เพราะอาจทำให้การกดปุ่ม, accessibility, สี และ animation เปลี่ยนไป

ไม่ควรแก้ไฟล์ `index.css` หรือ `poster-overrides.css` หากยังต้องการให้หน้าตาเหมือนเดิม เพราะสองไฟล์นี้เป็นส่วนควบคุมสี ฟอนต์ ระยะห่าง กรอบ และ responsive layout

## 10. ขั้นตอนหลังแก้ไข

หลังแก้ไฟล์ ให้ตรวจสอบ 4 อย่าง:

1. ไม่มีเครื่องหมายคำพูดที่เปิดแล้วไม่ปิด เช่น `"ข้อความใหม่` 
2. ไม่มี comma หายไปในรายการ `activities`, `rules` หรือ `gallery`
3. รูปภาพ URL เปิดได้จริง และเป็นไฟล์ที่อัปโหลดไว้ในระบบ
4. เปิดดูทั้ง desktop และ mobile เพื่อดูว่าข้อความยาวเกินการ์ดหรือไม่

ถ้าข้อความยาวมาก ควรย่อให้กระชับแทนการแก้ CSS เพราะการแก้ CSS อาจกระทบหน้าตาของการ์ดทุกใบ

## 11. สรุปแบบสั้นที่สุด

หากต้องการแก้เฉพาะข้อความ ให้ค้นหาข้อความเดิมใน `Home.tsx` แล้วแทนที่ข้อความด้านใน tag เดิม

หากต้องการแก้รูป ให้แก้ URL ใน `const ASSETS` หรือแก้ `image: ASSETS.water` / `image: ASSETS.elders` ของกิจกรรมนั้น

หากต้องการคงหน้าตาเดิม ให้แก้เฉพาะ **ข้อความ, URL รูปภาพ, `title`, `description`, `body` และ `details`** และคงโครงสร้าง JSX กับ `className` ทั้งหมดไว้
